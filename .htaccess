# ==========================================================================
# Alexo — configuration Apache (Hostinger)
# Ce fichier doit rester à la racine du site (public_html/).
# ==========================================================================

# --- Forcer HTTPS et le domaine sans www -----------------------------------
# À ACTIVER une fois le nom de domaine branché et le certificat SSL installé
# depuis le panneau Hostinger (retirer les # des 4 lignes ci-dessous).
# <IfModule mod_rewrite.c>
#   RewriteEngine On
#   RewriteCond %{HTTPS} off [OR]
#   RewriteCond %{HTTP_HOST} ^www\. [NC]
#   RewriteRule ^ https://alexo.fr%{REQUEST_URI} [L,R=301]
# </IfModule>

# --- Page d'erreur personnalisée -------------------------------------------
ErrorDocument 404 /404.html

# --- Compression -----------------------------------------------------------
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css text/plain text/xml
  AddOutputFilterByType DEFLATE application/javascript application/json
  AddOutputFilterByType DEFLATE image/svg+xml
</IfModule>

# --- Cache navigateur ------------------------------------------------------
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/html                 "access plus 0 seconds"
  ExpiresByType text/css                  "access plus 1 year"
  ExpiresByType application/javascript    "access plus 1 year"
  ExpiresByType image/svg+xml             "access plus 1 year"
  ExpiresByType image/webp                "access plus 1 year"
  ExpiresByType image/jpeg                "access plus 1 year"
  ExpiresByType image/png                 "access plus 1 year"
  ExpiresByType font/woff2                "access plus 1 year"
</IfModule>

# --- En-têtes de sécurité --------------------------------------------------
<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set Referrer-Policy "strict-origin-when-cross-origin"
  Header set X-Frame-Options "SAMEORIGIN"
</IfModule>

# --- Ne pas lister le contenu des dossiers ---------------------------------
Options -Indexes
